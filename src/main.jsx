import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import GPSScrollAnimation from "./GPSScrollAnimation";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GPSScrollAnimation />
  </React.StrictMode>
);